# CLAUDE.md — ngữ cảnh cho Claude Code khi làm việc trong repo này

Đọc `README.md` trước để hiểu tổng thể app. File này chỉ ghi thêm những điều **không hiển nhiên từ code**, các quyết định thiết kế đã tranh luận qua lại nhiều lần, và các lỗi đã sửa để tránh lặp lại.

## Nguyên tắc chung khi sửa app này
- **Chỉ 2 file `.md` trong repo: `README.md` (kiến trúc + "Việc còn dang dở") và `CLAUDE.md` (file này).** Đã từng có thêm `HANDOFF.md` làm bàn giao riêng — gây LỆCH THÔNG TIN thật với 2 file kia (vd HANDOFF.md giữ nguyên hướng dẫn dán `GEMINI_API_KEY` trần vào `config.js` dù kiến trúc đã đổi sang Supabase proxy từ lâu; 1 backlog viết bài đọc 46-Block chỉ nằm trong HANDOFF.md, README hoàn toàn không nhắc tới) — đã xoá HANDOFF.md, gộp hết vào 2 file này (2026-09-10). **Đừng tạo lại file bàn giao/notes riêng nào nữa** — việc dở dang ghi thẳng vào "Việc còn dang dở" trong README.md, quyết định/lỗi đã sửa ghi vào CLAUDE.md. Đừng tin trạng thái ghi trong bất kỳ file `.md` nào 100% nếu có REST API/code kiểm chứng nhanh được (vd trạng thái bài đọc — luôn verify qua Supabase, xem README).
- **Không có build step.** Sửa file trong `js/`/`css/`/`index.html` là có hiệu lực ngay, chỉ cần refresh trình duyệt (`localhost:8934`, server: `python3 -m http.server 8934` chạy từ thư mục `TJHUB`). Không bao giờ mở bằng `file://`.
- **Verify bằng Node trước khi giao việc**, đặc biệt với `context.js` (bài đọc/đề thi): `require()` thẳng file JS trong Node, shim `global.window`/`localStorage`/`fetch` tối thiểu cần thiết. Cách này đã bắt được nhiều bug thật (xem "Lỗi đã sửa" bên dưới) mà đọc code suông không thấy.
- Người dùng **rất dị ứng với nội dung tự sinh lặp lại/vô nghĩa** — đã từng phàn nàn nhiều lần về bộ mẫu câu cứng (OPENERS/MIDDLES/TITLES cố định trong `context.js`). Bộ này đã bị xoá hẳn (không còn là fallback). **Đừng thêm lại kiểu "mảng cố định chọn theo seed" cho bất cứ nội dung hiển thị nào** (tiêu đề, câu văn...) — nếu không có nội dung thật thì để trống, đừng bịa.
- Trước khi sửa dữ liệu người dùng (starter.json, hoặc bất kỳ export/import nào), **backup trước** (`cp file file.bak-$(date +%Y%m%d-%H%M%S)`).
- `data/starter.json` và `data/starter.version.json` có field `version` **PHẢI luôn khớp nhau tuyệt đối** — lệch 1 ký tự cũng gây bug vòng lặp "Thư viện có bản mới" vô hạn (đã xảy ra thật, xem lịch sử commit).
- **Mỗi lần đổi bất kỳ file `.js`/`.css` nào rồi push, PHẢI bump số `?v=N` của ĐÚNG file đó (và `css/app.css` nếu có sửa CSS) trong thẻ `<script>`/`<link>` tương ứng ở `index.html`** (`?v=1` → `?v=2`...). Không có build step tự động gắn hash/version, nên nếu quên bump thì trình duyệt/CDN của GitHub Pages có thể tiếp tục phục vụ file JS/CSS CŨ dù đã push code mới — gây đúng kiểu bug "sửa rồi báo user vẫn thấy y chang cũ, code trên GitHub đã đúng nhưng user không thấy" (đã xảy ra thật, tốn nhiều lượt qua lại mới tìm ra nguyên nhân là cache chứ không phải code sai). Chỉ cần bump ĐÚNG file mình sửa là đủ, không cần bump hết tất cả mỗi lần.
- **`GEMINI_API_KEY` KHÔNG còn tồn tại trong bất kỳ file client-side nào nữa** (`config.js`/`keys.local.js`) — đã bị Google tự động thu hồi **3 LẦN LIÊN TIẾP** trong ~24 tiếng (2026-09-09, rồi 2x 2026-09-10), kể cả sau khi giới hạn Application restrictions = Websites trong Cloud Console (restriction KHÔNG ngăn được secret-scanning thu hồi — nó chỉ giới hạn ai DÙNG ĐƯỢC key nếu lấy trộm). Giải pháp cuối cùng: chuyển hẳn sang **Supabase Edge Function `gemini-proxy`** (`supabase/functions/gemini-proxy/index.ts`) — key thật CHỈ nằm dưới dạng Supabase secret (server-side), client (web live lẫn máy local) chỉ gọi proxy qua `SUPABASE_URL`/`SUPABASE_ANON_KEY` sẵn có, không bao giờ cần biết giá trị key thật nữa nên không thể bị lộ qua git được nữa. `Context._callGemini` (`js/context.js`) đã đổi sang gọi proxy thay vì gọi thẳng Google. **ĐỪNG bao giờ dán GEMINI_API_KEY trần vào config.js/keys.local.js lại nữa** — nếu cần đổi key Gemini, chỉ chạy `supabase secrets set GEMINI_API_KEY=<key> --project-ref pqarpszsipbdugrumhfy` rồi thôi, không sửa file JS nào cả.
  - Deploy/redeploy proxy (cần `supabase login` trước — cần Personal Access Token từ https://supabase.com/dashboard/account/tokens nếu chạy không tương tác được):
    ```
    supabase secrets set GEMINI_API_KEY=<key thật> --project-ref pqarpszsipbdugrumhfy
    supabase functions deploy gemini-proxy --project-ref pqarpszsipbdugrumhfy --no-verify-jwt
    ```
  - Test nhanh sau khi deploy: `curl -s -X POST https://pqarpszsipbdugrumhfy.supabase.co/functions/v1/gemini-proxy -H "Content-Type: application/json" -d '{"model":"gemini-3.6-flash","sys":"Reply JSON only.","user":"Return {\"ok\":true}"}'` — phải trả JSON có `candidates[0].content.parts[0].text`, không phải lỗi 401/404.

## Kiến trúc tóm tắt (chi tiết xem README.md)
- `js/db.js` là lớp duy nhất biết dữ liệu nằm ở local hay Supabase (`DB.mode`). Code khác **không bao giờ** đọc `localStorage` trực tiếp hay gọi Supabase trực tiếp — luôn qua `DB.xxx()`.
- `S` (định nghĩa trong `app.js`, dùng chung sang `detail.js`) chỉ chứa dữ liệu của **Notebook đang mở** (`S.blocks`/`S.words`/`S.wp`/`S.bp`), KHÔNG phải toàn bộ app. Cần thống kê toàn app (vd Journey) phải gọi hàm riêng đọc thẳng `local()`/Supabase toàn cục (`DB.getJourneySummary`), không dùng `S`.
- Mỗi Block: `context_passage` (bài đang dùng, DUY NHẤT) + `context_passage_candidates` (mảng KHÔNG giới hạn số lượng bài Dán/Claude/OpenAI/Gemini đã lưu, chỉ để chọn thử — không tự áp dụng, xem mục "Nguồn bài đọc" bên dưới) + `vocab_fill_meta` (JSONB, cột thêm 2026-09-10 — `{provider, cost_usd, at, shared_with_blocks?}`, ghi lúc `Context.enrichWords` tự điền cột thiếu cho bảng từ vựng lúc dán từ mới ở `app.js` `doPaste()`, hiện badge cạnh "Danh sách từ vựng cần học" trong `detail.js` `D.renderStudy`).
- `Context.parseMeta(raw)` trả về `{marked, vi, title, source, ai, pasted, claude, provider, origin, cost_usd}` — 3 cờ `ai`/`pasted`/`claude` loại trừ lẫn nhau, dùng để: (a) hiện badge đúng nguồn gốc + chi phí bài đọc, (b) nhóm candidates theo nguồn thật trong `detail.js` (`groupCandidates`), (c) cleanup sweep trong `db.js` biết bài nào là "rác" (không cờ nào = mẫu cứng cũ, xoá).

## Lỗi đã sửa — đừng lặp lại
- `_markTerms` xử lý sai nếu từ ngắn là substring của từ dài hơn trong cùng danh sách (vd "cost" bên trong "irreversible cost") → **luôn sort terms dài nhất trước** khi bọc `[ngoặc]`.
- `gapSentences` dùng regex tách câu theo `.!?` bỏ sót đoạn cuối không có dấu câu kết thúc (transcript/bài báo dán vào hay bị cắt giữa chừng) → phải xử lý phần dư sau vòng lặp regex chính.
- `applyStarter()` (merge thư viện mẫu mới) từng **ghi đè luôn field `hub_id`/`notebook_id`/... của user** nếu user đã tự di chuyển Hub/Notebook/Section — phải so sánh field cha giữa bản local và bản fresh, giữ bản local nếu khác.
- `applyStarter()` cũng từng **hồi sinh item đã xoá** nếu chúng nằm trong thư viện mẫu mới — phải có "tombstone" (`tjwl_deleted_ids_v1`) lọc ra trước khi merge.
- `Context.parseMeta` từng khai báo field `pasted` khi TẠO bài dán nhưng **quên trả field đó ra** khi ĐỌC lại → badge "📝 Tự dán" không bao giờ hiện, âm thầm hỏng từ lúc thêm tính năng.
- PostgREST bulk insert (`POST .../rest/v1/<table>` với body là mảng) **yêu cầu MỌI object trong mảng có cùng bộ khoá tuyệt đối** (suy cột từ object đầu tiên) — object thiếu/thừa 1 key so với các object khác trong cùng mảng là lỗi `PGRST102 "All object keys must match"`. Luôn chuẩn hoá (fill đủ key, kể cả `null`/`[]`) trước khi insert hàng loạt.
- Đừng nhầm `sb_secret_...` (service_role, toàn quyền, chỉ dùng server) với `sb_publishable_...`/`anon` JWT (an toàn client-side) — Supabase dashboard mới hiển thị cả 2 loại key rất giống nhau về vị trí, dễ user gửi nhầm. Luôn **decode JWT payload kiểm tra `role: anon`** trước khi tin key người dùng gửi (base64 phần giữa 2 dấu `.`).
- `DB.getFullTree` từng chỉ check `DB.mode` (kho từ vựng Local/Cloud) để quyết định lấy `block_progress` ở đâu — nhưng tiến trình phải theo `progressLocal()` (`DB.mode==="local" || !DB.progressCloud`), vì kho có thể Cloud trong khi user chưa đăng nhập thật. Sai chỗ này gửi luôn id hồ sơ local (`us_xxxx`, không phải uuid) lên Supabase → lỗi `invalid input syntax for type uuid`, sập cả cây Journey. **Mọi hàm đụng tới `block_progress`/`word_progress` phải check `progressLocal()`, không phải `DB.mode`** — đã audit chéo, `DB.getJourneySummary`/`DB.loadProgress` làm đúng từ đầu, chỉ `getFullTree` bị sai.
- `gapSentences` (tách câu theo `.!?`) cắt nhầm tại dấu chấm của chữ viết tắt (Mr./Mrs./Dr./St....) — nếu 1 câu chứa `[term]` bắt đầu bằng "Mr. Ai đó, ...", câu điền-từ hiển thị ra sẽ bị cụt mất phần "Mr." phía trước. Chưa sửa regex (rủi ro động core parsing) — **khi viết bài đọc tay, tránh dùng chữ viết tắt có dấu chấm ngay trước/trong câu chứa `[term]`** (đổi "Mr. Kowalski" thành "a supervisor named Kowalski" chẳng hạn).
- `#pane-final` không tồn tại nhưng `D.showTab("final")` từng được gọi ở nút "🎯 Vào bài thi cuối bài →" (sau Active Recall Quiz) — `D.showTab` gọi `w.$("#pane-" + name).classList...` trên `null` → ném lỗi, tab hiện tại mất `.active` mà không tab nào bật thay, treo màn hình. 6 tab thật: study/quiz/sheet/single/meaning/progress — nút đó phải trỏ `"sheet"` (Phiếu đầy đủ).

## Quyết định đã chốt (đừng đề xuất lại)
- **KHÔNG** có UI chọn độ khó Dễ/Vừa/Khó cho bài đọc (đã làm rồi bị yêu cầu bỏ — "hong có tác dụng"). Thay vào đó là 1 hàng tab **"📝 Dán" + "Dán 1/2/3…" + "Claude 1/2/3…" + "OpenAI 1/2/3…" + "Gemini 1/2/3…"** (`#src-tabs`/`#src-body` trong `detail.js`, hàm `D.renderSourcePicker`) — LUÔN hiện (không ẩn khi đã có bài đọc), chọn tab nào xem trước tab đó rồi bấm chung 1 nút "Dùng bài này" mới đẩy lên. Không có nút "Nhờ AI viết" riêng ở khu này — dùng "🔄 Tạo lại" ở đầu card bài đọc. **"📝 Dán" (không số) là khung soạn/dán trực tiếp, chưa lưu**; các tab có số (Dán 1/2/3…, Claude 1/2/3…, OpenAI 1/2/3…, Gemini 1/2/3…) là các khe ĐÃ LƯU trong `context_passage_candidates`, nhóm theo NGUỒN THẬT đọc từ `parseMeta()` của từng phần tử (`pasted`/`claude`/`provider`), **không theo vị trí trong mảng** — xem `groupCandidates()`/`addCandidate()` trong `detail.js`. **KHÔNG giới hạn số lượng bài/nguồn** (đã thử chốt 3/nguồn rồi user yêu cầu bỏ giới hạn — "cứ nào mà gen xong, dán xong bấm dùng thì lưu ra tiếp theo") — bài mới luôn nối vào CUỐI đúng nhóm, bài cũ không bao giờ tự mất, chỉ mất khi bấm tay "🗑 Xoá bài này" (`D.deleteCandidateTab`, ẩn ở tab "📝 Dán" vì đó là khung soạn chưa lưu). "🔄 Tạo lại" (AI) và "Dùng bài này" (khi đang ở tab dán) đều vừa áp dụng ngay `context_passage` VỪA lưu thêm vào đúng khe nguồn tương ứng. `D._srcTab` là chuỗi `"paste"` hoặc `"<nhóm>:<idx>"` (vd `"openai:0"`), không phải số nguyên chỉ số phẳng. Mỗi bài AI sinh có `meta.cost_usd` (ước tính USD OpenAI đã tốn, đọc từ `usage` token thật trả về — xem `OPENAI_PRICING` trong `context.js`, Gemini luôn `null`/0) hiện ngay cạnh badge nguồn (`#passage-cost` khi đang active, hoặc trong phần xem trước ở `#src-body`) — vì `context_passage` khi "Dùng bài này" LUÔN là bản copy y hệt string của candidate (kể cả phần meta ẩn), nên nguồn+chi phí tự động đúng ngay, không cần code riêng khi áp dụng. Cạnh nút "🔄 Tạo lại" có thêm `<details id="regen-prompt-box">` — ô prompt gốc (`Context.DEFAULT_PROMPT_TEMPLATE`, có placeholder `{{SETTING}}/{{STYLE}}/{{DIFF}}/{{WORDLIST}}`) tự prefill lúc mở Block, sửa gì cũng được trước khi bấm Tạo lại (đọc thẳng textarea làm `promptOverride` cho `generateAI`), có nút "↺ Về mặc định" để reset.
- **KHÔNG** tự động luân phiên bài đọc theo chu kỳ ôn (đã thử, bị thay bằng picker thủ công ở trên).
- **KHÔNG** tự fallback về mẫu câu cứng khi chưa có AI/chưa dán bài — thà để trống.
- Số Block là `global_index`, **KHÔNG trùng nhau xuyên suốt cả Notebook** (đã thử đánh lại từ 1 mỗi Batch rồi bị yêu cầu revert — "để dễ nhớ/dễ nhắc tên 1 Block cụ thể"). Vị trí (Batch/Page nào) đã có breadcrumb lo, không cần nhét vào tên Block.
- "Done" trên Block = **1 trong 3** thẻ bài tập đạt ≥80% — và từ giờ **CẢ 3 THẺ ĐỀU đẩy chu kỳ SRS thật** (`bp.passed`), kể cả Nghĩa (đã đổi: trước đây chỉ Phiếu đầy đủ/Từng câu mới đẩy SRS, Nghĩa chỉ set `meaning_passed` — user yêu cầu đổi vì thấy mâu thuẫn "Done nhưng chưa vào chu kỳ ôn").
- **Học sớm (trước `next_review_at`) vẫn được và vẫn ghi điểm, nhưng KHÔNG đẩy chu kỳ ôn lên sớm** (`srsAdvanceIfDue()` trong `detail.js`) — tránh cày nhiều lần trong ngày để nhảy cóc lịch Tony Buzan. Có nới 1 tiếng (`SRS_GRACE_MS`): ôn sớm hơn hạn ≤1 tiếng vẫn tính đúng hạn.
- Mỗi lần THẬT SỰ đẩy chu kỳ được ghi vào `bp.review_history` ([{step, at}]) — tab Tiến trình dùng cái này để hiện đúng ngày giờ đã ôn từng lần, không chỉ dấu ✓ chung chung.
- Từng câu/Nghĩa: câu cuối trả lời xong (đúng hay sai) là **tự chấm luôn**, không cần nút "Nộp bài" — chỉ Phiếu đầy đủ giữ nút Nộp bài tường minh.
- Đọc bài/bảng từ vựng: **user tự cuộn đi đâu thì để yên hẳn** cho tới lần bấm nghe tiếp theo (`speech.js` — cờ `userTookControl`, không phải hẹn giờ 1.5s như trước) — chữ vẫn sáng theo giọng đọc, chỉ riêng việc tự cuộn màn hình là dừng.
- **Repo đã chuyển PUBLIC** (từ private) để dùng GitHub Pages miễn phí — đã rà soát kỹ toàn bộ lịch sử git, không có key/credential nhạy cảm nào (xem README.md mục Triển khai). Host chính giờ là GitHub Pages (`https://teoteo1081.github.io/tj-wordloop-hub/`, tự build khi push, không cần lệnh deploy riêng); Netlify giữ làm dự phòng.
- Nút "Hiểu rồi, vào kiểm tra →" (sau khi đọc bài) mặc định mở tab **"Nghĩa"** (không phải Từng câu/Phiếu đầy đủ) — đã đổi qua lại vài lần trong lịch sử, chốt cuối cùng là Nghĩa.
- **KHÔNG** làm "dashboard lọc Block theo trạng thái" (bấm 1 con số trong ô cảnh báo — vd "44 block chưa học" — để lọc thẳng `#blocks-list` theo trạng thái đó) — đã làm thử đầy đủ (kèm dropdown "Sắp xếp: theo số thứ tự / theo trạng thái ôn"), user thấy **rườm rà**, yêu cầu bỏ hết, quay lại `renderAlert`/`App.renderBlocks` nguyên bản (số liệu tĩnh, không bấm được). Đừng đề xuất lại kiểu tương tác này cho riêng phần Batch/Block-list — nếu muốn xem Block theo trạng thái thì đã có sẵn 4 tab trong Journey (xem mục Journey ở README.md).
- **KHÔNG** đặt mũi tên "Batch trước/sau" cạnh tiêu đề "Batch 1" ở đầu màn danh sách Block (`#screen-blocks .screen-head`) — đã làm thử, user báo sai vị trí/rối, yêu cầu bỏ. Mũi tên Batch trước/sau CHỈ nên đứng ở vị trí cũ: `.bn-buttons`/chip Batch trong `.batches-bar` (hàng trên cùng, cạnh nút Paste). Vẫn giữ **logic** `App.flattenBatches()`/`App.stepBatch(dir)` (duyệt Batch xuyên Page/Notebook trong cùng Hub) — chỉ bỏ phần UI đặt sai chỗ; logic này đang được dùng cho nút Block trước/sau trong Chi tiết Block (`D.gotoAdjacentBlock`).
- Batch đang chọn có hiện thêm vào breadcrumb trên cùng (`#crumb`, `renderCrumb()` trong `app.js`) — quyết định NÀY vẫn giữ (không bị revert cùng đợt dashboard lọc ở trên). Breadcrumb quá dài (nhiều cấp, tên dài) thì tự rút gọn còn "Hub › … › cấp đang đứng" thay vì xuống hàng — xem `CRUMB_MAX_CHARS` trong `app.js`.
- **ĐÃ BỎ HẲN** thanh "đánh máy" từ đã học (`js/ticker.js` + `.word-ticker`/`.wt-*` trong app.css) — từng thêm, qua vài lần chỉnh, nhưng user báo **lỗi hiển thị**, yêu cầu bỏ hẳn (không phải tạm ẩn). Đã xoá cả file `js/ticker.js`, thẻ `<script>`, HTML `#word-ticker`/`#wt-toggle`, CSS liên quan, và dòng gọi `w.Ticker.refresh()` trong `app.js`. Đừng đề xuất lại tính năng này.
- `Context.generateAI(words, cfg, difficulty, promptOverride, topicHint)`: `difficulty` để `null` thì **TỰ RANDOM** 1 trong easy/medium/hard mỗi lần gọi (không có UI chọn — UI đó đã bị bỏ trước đây, đây là random ngầm để đa dạng hoá, theo yêu cầu "đa dạng hoá khó khó xíu"). `topicHint` (vd `App.currentTopicHint()` = tên Notebook + Section, "Digital Marketing — Landing pages") ép AI lấy đúng lĩnh vực đó làm **NỘI DUNG CHÍNH**, còn `SETTING` random (sân bay, tiệc gia đình...) chỉ còn là khung cảnh/nhân vật bao quanh — **PHẢI viết rõ "CHỦ ĐỀ NỘI DUNG CHÍNH... PHẢI xoay quanh"** chứ không phải câu gợi ý nhẹ kiểu "nên liên quan", nếu không model sẽ ưu tiên đúng chữ "BẮT BUỘC" của SETTING và bỏ qua topicHint hoàn toàn (đã test thật, xem lịch sử commit "Thêm topicHint + random difficulty").

- **Tab "🎧 Dictation" (nghe câu, gõ lại, tự chấm text) là luyện tập THUẦN TUÝ** — KHÔNG ghi vào `block_progress`/`word_progress`, không đẩy chu kỳ SRS, không tính vào "✓ Done"; thoát tab là mất kết quả phiên đó. Đây là quyết định phạm vi có chủ đích (không phải làm dở) — nếu muốn đổi sang có ghi điểm (giống "Nghĩa": `dictation_passed`/`dictation_best`, không đẩy SRS) thì phải hỏi người dùng trước, xem code mẫu ở `D.submitMeaning` (`detail.js`).
- **Có 2 profile `is_admin=true`**: "TJ" và "Anti_TJ" — dễ nhầm lúc test/debug quyền Admin, luôn kiểm tra đang đăng nhập profile nào trước khi kết luận 1 bug liên quan tới quyền.

## Khi viết bài đọc tay hàng loạt (nếu được yêu cầu tiếp)
1. Lấy đúng danh sách 10 từ của Block — query thẳng Supabase REST API là nhanh nhất (xem lệnh mẫu trong README.md mục "Việc còn dang dở"), không cần export JSON của user.
2. Viết ~480-520 từ tiếng Anh, 3-6 đoạn (dòng trống ngăn cách), mỗi từ trong 10 từ xuất hiện ĐÚNG 1 LẦN, NGUYÊN VĂN (không chia động từ/số ít-nhiều), bọc `[term]`. Ưu tiên MỖI TERM 1 CÂU RIÊNG (không dồn 2 term vào 1 câu) — câu điền-từ sẽ gọn hơn. **Tránh chữ viết tắt có dấu chấm (Mr./Mrs./Dr./St./vs....) ngay trước hoặc trong câu chứa `[term]`** (xem "Lỗi đã sửa" — làm gãy sentence-splitter). Đổi bối cảnh/nhân vật giữa các bài để đỡ lặp — không phải lúc nào cũng là "họp hành văn phòng".
3. Với MỖI câu chứa 1 `[term]`, viết bản dịch tiếng Việt của ĐÚNG câu đó (nguyên câu, không phải cả đoạn) vào `meta.vi[term.toLowerCase()]`. Đặt `meta.title`, `meta.source` (1 dòng tiếng Việt mô tả), `meta.claude = true`.
4. **Bắt buộc verify bằng Node trước khi đẩy lên** — `require()` thẳng `js/context.js` (shim `global.window`, `window.esc`):
   - `Context.gapSentences(marked).length === 10`, đúng khớp cả 10 term của Block, không thiếu/thừa/trùng.
   - Đếm chữ bằng `Context.build(marked).plain.trim().split(/\s+/).length` — nếu < 450 thì THÊM CÂU MÔ TẢ/KHÔNG-TERM vào các đoạn (không đụng câu có `[term]`) rồi đo lại, đừng nộp bài quá ngắn.
   - `Context.translate(g.text, g.term, null, meta.vi)` phải trả về khác rỗng cho MỌI gap (vòng lặp qua `gapSentences()`), tức `meta.vi` khớp đủ, đúng key.
   - `Context.parseMeta(marked + Context.META_SEP + JSON.stringify(meta)).marked === marked` (round-trip không lệch).
5. Ghi vào `context_passage_candidates` (mảng, **không giới hạn số phần tử** — mỗi phần tử là `marked + Context.META_SEP + JSON.stringify(meta)`, `meta.claude = true` để nhóm đúng vào tab "Claude" trong picker), **KHÔNG** ghi thẳng vào `context_passage` trừ khi user xác nhận Block đó đang trống và muốn dùng luôn. Đẩy lên bằng `PATCH .../rest/v1/blocks?id=eq.<id>` (body `{"context_passage_candidates":[...]}`, header `apikey`+`Authorization: Bearer <anon key>`), **ghi đè cả mảng** — Block đã có sẵn candidate cũ (Claude/OpenAI/Gemini/Dán) thì phải GET trước rồi NỐI THÊM vào cuối, không PATCH đè mất bài cũ.
6. Nếu build file JSON để user import: **luôn dùng bản export MỚI NHẤT làm gốc** (`📂/💾` trong app là THAY THẾ TOÀN BỘ, không merge) — hỏi lại nếu nghi ngờ user đã tự sửa gì từ lúc export cũ, đừng đè mất.
