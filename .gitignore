# Bí mật cá nhân — KHÔNG đưa lên git/GitHub dưới bất kỳ hình thức nào
js/keys.local.js

# Báo cáo người học do tools/manage_users.py xuất ra - CHỨA LINK ĐĂNG NHẬP
# THẬT (mở là vào thẳng tiến trình của người đó, không cần email/mật khẩu)
# - tuyệt đối không đẩy lên repo public này.
tools/reports/

# Excel từ vựng/bài đọc do tools/sync_vocab.py xuất ra - file làm việc cục
# bộ (không nhạy cảm nhưng cũng không cần versioning ở repo public này).
tools/tu_vung.xlsx

# Local Netlify folder
.netlify

# Backup snapshots tự tạo trước khi sửa dữ liệu (xem CLAUDE.md)
*.bak-*
.DS_Store
